import { Entitlementstotransport } from './entitlementstotransport';

describe('Entitlementstotransport', () => {
  it('should create an instance', () => {
    expect(new Entitlementstotransport()).toBeTruthy();
  });
});
